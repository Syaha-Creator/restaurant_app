import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0094C6);
  static const Color secondary = Color(0xFF005E7C);

  static const Color backgroundLight = Colors.white;
  static const Color backgroundDark = Color(0xFF000022);
  static const Color backgroundSecondary = Color(0xFF001242);

  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF040F16);

  static const Color textLight = Colors.black;
  static const Color textDark = Colors.white;
  static const Color textSecondary = Color(0xFF005E7C);

  static const Color accent = Color(0xFF0094C6);
  static const Color error = Colors.redAccent;
}
